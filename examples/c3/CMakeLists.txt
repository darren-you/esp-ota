cmake_minimum_required(VERSION 3.16)
set(PROJECT_VER "0.1.0-lab")
if(EOTA_LAB_ARMED AND EOTA_LAB_INPUTS)
    get_filename_component(_eota_lab_input_real "${EOTA_LAB_INPUTS}" REALPATH)
    get_filename_component(_eota_build_real "${CMAKE_BINARY_DIR}" REALPATH)
    string(FIND "${_eota_lab_input_real}" "${_eota_build_real}/" _eota_build_prefix)
    if(_eota_build_prefix EQUAL 0)
        message(FATAL_ERROR "EOTA_LAB_INPUTS must be outside the build directory")
    endif()
endif()
set(EXTRA_COMPONENT_DIRS "${CMAKE_CURRENT_LIST_DIR}/../../components/esp_ota")
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp_ota_lab)
