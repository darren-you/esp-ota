- 将锁定 ESP-IDF 切至公开修复 fork，并复验签名 C3 样例
- 避免 OTA 期限回调同步等待 TCP/IP 线程并核清墙钟边界
- 删除冗余期限定时器，统一使用单调时钟绝对期限
- 收紧 OTA 单次 TLS 读写期限并阻止逾期 Flash 阶段继续切槽
- 将 OTA 准备期限覆盖槽预检并拒绝逾期预检后的下载
- 增加已验签固件完整镜像摘要的只读观察接口与故障回归
- 选槽前按当前可信产品约束复验目标 Flash 镜像头
- 约束 TLS 1.3 连续会话票据的单次读取期限并补回归
- 精确锁定修复 HTTP 初始化失败句柄泄漏的公开 ESP-IDF fork
- 拒绝位于构建目录内的实验输入以防配置覆盖并误判签名样例
